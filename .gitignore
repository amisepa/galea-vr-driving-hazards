node_modules/
*.docx
*.pdf

# MATLAB console output
results_final/**/*.log
*.log

# Internal working notes (kept on Proton Drive)
TODO.md

# MATLAB autosave
*.asv

# Large intermediate analysis output — regenerate with the run_final_* scripts.
# The permutation null arrays are ~36 MB each and are not needed to reproduce
# the reported results; the cluster CSVs and figures are committed instead.
results_final/**/PRESTIM_permutation_output.mat
results_final/**/PERM_output.mat
results_final/**/*_stats*.mat
results_final/**/TF_stats_*.mat
results_final/**/COV_stats.mat

# TF power caches (~0.5-1 GB each). Archived on Proton Drive under
# code_archive/results_superseded/ — the run_final_EEG_tf_causal.m and
# run_final_EEG_tf_ascent.m scripts regenerate them from the study data.
results_final/**/*_power_cache.mat

# MATLAB figure sources (the .png versions are committed)
results_final/**/*.fig

# Superseded results folders (kept only in git history / tag pre-repair-results)
results/
results_new/
results_TF_new/
results_final/EEG_tf/
results_final/EEG_tf_ascent/
